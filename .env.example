# Copiar como .env y completar valores reales
# NUNCA subir .env a Git

SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sgi_system
DB_APP_USER=sgi_admin
DB_APP_PASSWORD=

# JWT (Historia 1.4)
JWT_SECRET=
JWT_EXPIRATION_MS=3600000
JWT_REFRESH_DAYS=30